{
  "name": "rtb-glb-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone GLB optimization pipeline (mesh simplify + quantize + Meshopt + WebP). Run as a forked child process by the API so its heap is isolated from rtb-api.",
  "bin": {
    "rtb-optimize-glb": "./optimize.mjs"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@gltf-transform/core": "^4.4.2",
    "@gltf-transform/extensions": "^4.4.2",
    "@gltf-transform/functions": "^4.4.2",
    "meshoptimizer": "^1.2.0",
    "sharp": "^0.35.4"
  }
}
